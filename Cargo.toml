[workspace]
resolver = "2"
members = [ "balking", "future","guarded-suspension", "immutable", "producer-consumer", "read-write-lock", "single-threaded-execution", "thread-per-message", "thread-specific-storage", "two-phase-termination", "worker-thread"]

[workspace.dependencies]
rand = "0.8.5"
